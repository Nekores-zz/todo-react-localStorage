import styled from "styled-components"

export const TodoWrapperStyle = styled.div`
  p.errorMessage {
    color: red;
    position: relative;
    top: -11px;
    font-size: 15px;
    margin-bottom: -10px;
  }
`
